 (if (fboundp 'normal-top-level-add-subdirs-to-load-path)
        (let* ((my-lisp-dir "~/Dropbox/crossconf/emacs.d/")
              (default-directory my-lisp-dir))
           (setq load-path (cons my-lisp-dir load-path))
           (normal-top-level-add-subdirs-to-load-path)
	   (progn (cd my-lisp-dir) (load-file '"hmoo.el"))))

;;(add-to-list 'load-path '"~/Dropbox/crossconf/emacs.d")
;;(load-file '"~/Dropbox/crossconf/emacs.d/hmoo.el")

