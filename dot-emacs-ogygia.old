;(prefer-coding-system 'utf-8-auto)

;(add-to-list 'load-path "~/Dropbox/crossconf/emacs.d")
;(add-to-list 'load-path "~/Dropbox/crossconf/slime/")
;(add-to-list 'load-path "~/Dropbox/crossconf/dictionary-1.8.7/")


(load "hmoo-conf.el")




;    (require 'command-frequency)
;     (setq 'command-frequency-table-file "~/.emacs.frequencies")
;    (command-frequency-table-load)
;    (command-frequency-mode 1)
;     (command-frequency-autosave-mode 1)



(setq make-backup-files t) 
(setq version-control t) 
(setq kept-old-versions (+ kept-old-versions 2)) 
(setq kept-new-versions 5) 
(setq backup-directory-alist '(("." . "~/Dropbox/emacs-backups/")))



;(require 'desktop)
;(load ".local.el" t)


(put 'narrow-to-region 'disabled nil)

;; rcirc-server-alist
;; (("irc.freenode.net" :channels ("#rcirc")))
;; (setq rcirc-server-alist '(("eu.undernet.org" :channels ("#bookz" "#creative-minds"))))
;; (("eu.undernet.org" :channels ("#bookz" "#creative-minds")))

;; (("eu.undernet.org" :channels ("#bookz" "#creative-minds")))



;; (add-hook 'org-mode-hook					   ;;
;; 	 (lambda ()						   ;;
;; 	   (make-variable-buffer-local 'yas/trigger-key)	   ;;
;; 	   (setq						   ;;

      ;; (autoload 'dictionary-search "dictionary" 
      ;;   "Ask for a word and search it in all dictionaries" t)
      ;; (autoload 'dictionary-match-words "dictionary"
      ;;   "Ask for a word and search all matching words in the dictionaries" t)
      ;; (autoload 'dictionary-lookup-definition "dictionary" 
      ;;   "Unconditionally lookup the word at point." t)
      ;; (autoload 'dictionary "dictionary"
      ;;   "Create a new dictionary buffer" t)
      ;; (autoload 'dictionary-mouse-popup-matching-words "dictionary"
      ;;   "Display entries matching the word at the cursor" t)
      ;; (autoload 'dictionary-popup-matching-words "dictionary"
      ;;   "Display entries matching the word at the point" t)
      ;; (autoload 'dictionary-tooltip-mode "dictionary"
      ;;   "Display tooltips for the current word" t)
      ;; (unless (boundp 'running-xemacs)
      ;; 	  (autoload 'global-dictionary-tooltip-mode "dictionary"
      ;; 	    "Enable/disable dictionary-tooltip-mode for all buffers" t))
      ;; (load "dictionary-init")
;;    Ido

;(add-to-list 'ido-ignore-files "\.DS_Store")


;;load haskell-mode from wherever


